<?php
/**
 * ОБРАЗЕЦ. Скопируйте этот файл рядом под именем config.local.php
 * и впишите пароль почтового ящика.
 *
 * Зачем отдельный файл: его нет в архивах с обновлениями сайта.
 * Заливка новой версии его не трогает, поэтому пароль не пропадает.
 * Всё остальное лежит в mail.php и обновляется вместе с сайтом.
 *
 * Что здесь указано — то и работает; что не указано — берётся из mail.php.
 */

// Пароль ящика robot@shtukatur74.ru — единственное, что нужно заполнить.
define('SMTP_PASS', 'сюда-пароль-ящика');

// Ключ для страницы проверки /api/mail-test.php?key=...
// Пусто — страница отвечает 404 и ничего не отправляет.
define('MAIL_TEST_KEY', '');

/* ------------------------------------------------------------------
   Ниже — на случай, если настройки хостинга отличаются от REG.RU
   по умолчанию. Раскомментируйте нужные строки, убрав «//» в начале.

// define('SMTP_HOST', 'mail.hosting.reg.ru');
// define('SMTP_PORT', 587);
// define('SMTP_SECURE', 'tls');     // ssl для порта 465, tls для 587
// define('SMTP_USER', 'robot@shtukatur74.ru');

// Куда приходят заявки
// define('MAIL_TO', 'robot@shtukatur74.ru');
------------------------------------------------------------------ */
